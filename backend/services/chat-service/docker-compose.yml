# Build and run ONLY chat-service from this folder:
#   docker compose up -d --build
#
# 1) Start infrastructure first (once):
#      cd ../../infrastructure && docker compose up -d
# 2) Then from this folder:
#      docker compose up -d --build
#
# Or from backend root:
#   .\scripts\docker-build-service.ps1 chat-service -Up

services:
  chat-service:
    build:
      context: ../..
      dockerfile: services/chat-service/Dockerfile
      args:
        SERVICE_PORT: 8087
    image: vithey-chat-service:local
    container_name: vithey-chat-service
    env_file:
      - .env.example
    ports:
      - "8087:8087"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8087/actuator/health | grep -q '\"status\":\"UP\"'"]
      interval: 15s
      timeout: 5s
      start_period: 90s
      retries: 12
    networks:
      - vithey-network
    restart: unless-stopped

networks:
  vithey-network:
    external: true
    name: vithey-network