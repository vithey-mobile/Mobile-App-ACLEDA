spring:
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}
  jpa:
    show-sql: false

eureka:
  client:
    enabled: ${EUREKA_CLIENT_ENABLED:false}

logging:
  level:
    com.vithey.auth: DEBUG
