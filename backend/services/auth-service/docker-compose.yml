# Build and run ONLY auth-service from this folder:
#   docker compose up -d --build
#
# 1) Start infrastructure first (once):
#      cd ../../infrastructure && docker compose up -d
# 2) Then from this folder:
#      docker compose up -d --build
#
# Or from backend root:
#   .\scripts\docker-build-service.ps1 auth-service -Up

services:
  auth-service:
    build:
      context: ../..
      dockerfile: services/auth-service/Dockerfile
      args:
        SERVICE_PORT: 8081
    image: vithey-auth-service:local
    container_name: vithey-auth-service
    env_file:
      - .env.example
    ports:
      - "8081:8081"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8081/actuator/health | grep -q '\"status\":\"UP\"'"]
      interval: 15s
      timeout: 5s
      start_period: 90s
      retries: 12
    networks:
      - vithey-network
    restart: unless-stopped

networks:
  vithey-network:
    external: true
    name: vithey-network