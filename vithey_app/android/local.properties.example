## Copy values into android/local.properties (gitignored). Do not commit secrets.

# sdk.dir=...
# flutter.sdk=...

# Required for Google Maps on Android (Maps SDK key, not Places server key)
GOOGLE_MAPS_API_KEY=YOUR_ANDROID_MAPS_SDK_KEY
