# Merge Ayheng + Molika Frontend

- Create/switch to branch `ayheng-merge`.
- Use `molika` as the base/main code because it is already updated from Kimheang's main project.
- Compare both branches:
  - `molika`
  - `ayheng`
- Merge Ayheng's changes into `ayheng-merge` while keeping Molika's code as the base.

Requirements:
- Check all differences between `ayheng` and `molika`.
- Preserve useful updates from both branches.
- Resolve conflicts carefully instead of blindly choosing one side.
- If both branches modified the same code, compare the changes and combine them when possible.
- Do not remove working features from either branch.
- Keep the final code consistent and working with the existing project structure.
- Do not modify `ayheng` or `molika`; only make changes in `ayheng-merge`.
- After merging, check for broken imports, duplicated code, missing features, and obvious build/runtime errors.
