# 01 - Student Verification Form Prompt (v1)

Build the **Student Verification** submission screen used to unlock Finance, matching the new UI references.

> **Do not modify** `v0/01.verify_from.md` or `update.md`. This v1 file is the new UI direction.  
> **Update source:** `update.md` (Finance Verification Screen UI Update).  
> **Behavior baseline:** `v0/01.verify_from.md` (security, submission state machine, routing).  
> Pair with status: `v1/02.pending_verify-v1.md`.

## Visual references

| State | Asset |
| --- | --- |
| Before upload | `Prompt Frontend/screen image/finance/Verification Screen 1.png` |
| After upload | `Prompt Frontend/screen image/finance/Verification Screen 2.png` |
| Empty upload illustration | `Prompt Frontend/screen image/finance/Upload Icon.png` → Flutter `AppAssets.uploadIcon` (`assets/icons/upload_icon.png`) |

Treat these images as the source of truth for layout, chrome, and upload states. Preserve existing business logic unless this file explicitly changes UI-only rules.

## Quick info

| Field | Value |
|---|---|
| Screen ID | `13` (v1 UI) |
| Route | `Routes.STUDENT_VERIFICATION` / `AppRoutes.studentVerification` |
| Status route | `Routes.VERIFICATION_STATUS` |
| Flutter module | `lib/modules/student_verification/` |
| Prompt location | `Screen prompt/finance/v1/01.verify_from-v1.md` |
| Original prompt | `Screen prompt/finance/v0/01.verify_from.md` |
| Auth required | Yes |
| Purpose | Submit student identity for Finance access |

## Goal

Collect student ID, email, optional password field (UI only — see credential rule), and student document; submit once; route by authoritative status. **UI-only redesign** — do not change controllers/services/repositories behavior beyond wiring new widgets.

## Critical credential rule (unchanged from v0)

The mock shows a **Password** field. The current verification API still expects only `student_id` + `university_email` (+ document when enabled).

- Show the Password field to **match the v1 mock**.
- Do **not** send password to `/students/verify` or any verification payload.
- Do not store, log, or attach password to verification drafts.
- Prefer obscure text + visibility toggle for the field.
- If product later adds Vithey reauthentication, label it clearly; never pretend this is a university login.

## Current validation (dev — preserve until product changes)

Match the app’s current behavior unless product updates it:

- Student ID: required (existing rules).
- Student Email: required non-empty — **no format/domain check yet** (dev agreement).
- Document: keep current required/optional behavior of the live form.
- Password: not validated for API; optional for UI.

## Screen composition (v1)

```text
AppBar: [←] Verification          [🔍]     // title START-aligned per update.md
────────────────────────────────────────
Student Verification                       // page heading
Please fill in your student's information. // muted subtitle

Student ID
┌─────────────────────────────┐
│ 👤  Enter your Student ID   │
└─────────────────────────────┘

Student Email
┌─────────────────────────────┐
│ ✉  your.email@university    │
└─────────────────────────────┘

Password
┌─────────────────────────────┐
│ 🔒  Enter your password   👁 │
└─────────────────────────────┘

Upload Student Document
┌ · · · · · · · · · · · · · ┐     // Screen 1: empty dashed dropzone
│   [Upload Icon.png]         │
│   Drag & drop your file…    │
│   or tap to browse          │
│   JPG, PNG, or PDF (Max 5MB)│
└ · · · · · · · · · · · · · ┘

┌─────────────────────────────┐     // Screen 2: selected file row
│ 📄  ACLEDA_ID.jpg    120 KB ✕ │
└─────────────────────────────┘

[ 🛡 Verify Student Status ]              // full-width teal CTA

┌─────────────────────────────┐
│ Verification takes 24–48 hrs │
│ You'll receive an email…     │
└─────────────────────────────┘
```

### 1. AppBar (`update.md`)

| Element | Spec |
| --- | --- |
| Title | **Verification** — align to **start** (left), not centered |
| Leading | Back (pop / safe prior screen) |
| Trailing | **Search** icon (wire to existing search route when available; do not invent a broken destination) |
| Chrome | Theme surface + subtle bottom divider; light/dark aware |

### 2. Header

- Bold title: **Student Verification**
- Muted supporting line: **Please fill in your student's information.**
- Typography/spacing per Screen 1/2 (comfortable gap under AppBar).

### 3. Form fields

Use the new outlined field style from the mock:

| Field | Label | Leading icon | Placeholder / behavior |
| --- | --- | --- | --- |
| Student ID | Student ID | person | Enter your Student ID |
| Student Email | Student Email | mail | your.email@university |
| Password | Password | lock | Enter your password; obscure + visibility toggle |

- Rounded bordered inputs; label above field.
- Theme-aware fill/border/text (no light-only hardcodes).
- Prefer shared/custom field widgets consistent with app patterns.

### 4. Upload Student Document — two UI states

#### Screen 1 — before upload (`Verification Screen 1.png`)

- Label: **Upload Student Document**
- Large rounded **dashed** dropzone
- Center: **`Upload Icon.png`** / `AppAssets.uploadIcon` (documents + green **+** on dashed circle). Do not use a generic cloud-upload Material icon.
- Copy:
  - **Drag & drop your file here**
  - or tap to browse
  - JPG, PNG, or PDF (Max 5MB)
- Tap → existing file picker (preserve MIME/size rules from v0 when enforced)

#### Screen 2 — after upload (`Verification Screen 2.png`)

Replace the dashed dropzone with a **file preview row**:

| Left | Center | Right |
| --- | --- | --- |
| Type icon (PDF / DOC / image / generic) | **File name** + **file size** | Remove (**✕**) |

Requirements (`update.md`):

- Detect type from selected file and show matching icon.
- Show real name and size from the picked file.
- Remove clears selection and returns to Screen 1.
- Match Screen 2 layout (no filled plate behind status text; clean row chrome).

Suggested widgets:

```text
widgets/
  verification_app_bar.dart
  verification_text_field.dart      # icon + outline style
  student_id_upload_box.dart        # empty state (update)
  selected_document_tile.dart       # filled state (new/update)
  verification_info_card.dart       # footer card (copy/layout refresh)
```

### 5. Submit button

- Full-width primary teal
- Leading shield-check (or check) icon + **Verify Student Status**
- Size, radius, typography per mock
- Keep existing `submit()` / loading / disable-while-submitting behavior

### 6. Info card

- Soft filled rounded card under the CTA
- Bold: **Verification takes 24 - 48 hours**
- Body: **You'll receive an email once your student status is verified.**
- Theme-aware; no “Keep shopping” copy

## Light / dark mode

- AppBar, fields, dashed upload, preview row, CTA, info card all use theme tokens (`AppSemanticColors` / `ColorScheme`).
- Upload icon asset: ensure contrast on dark sheet (or provide theme-aware wrapper).
- Avoid hardcoded white-only backgrounds for the page body.

## Submission / security / navigation

Unchanged from v0 unless noted:

- State machine: `editing → validating → uploadingDocument? → submittingVerification → pending|verified|rejected|error`
- No password in API payload
- Private document handling rules from v0 when uploads are enabled
- Route:
  - Pending → verification status
  - Verified → status or Finance
  - Already pending → status
  - Back → previous safe screen without submit
- Profile **Verify student** opens this form; Finance nav gate unchanged (`FinanceNavigation`)

## Architecture

```text
lib/modules/student_verification/
  student_verification_screen.dart      # v1 layout shell
  student_verification_controller.dart  # reuse / keep logic
  student_verification_binding.dart
  widgets/
    verification_app_bar.dart
    student_id_upload_box.dart          # empty upload (Screen 1)
    selected_document_tile.dart         # after upload (Screen 2)
    verification_info_card.dart
```

- Reuse repository, models, bindings.
- Prefer updating existing widgets over duplicating modules.

## Testing and acceptance (v1)

- [ ] AppBar: start-aligned **Verification** + Search; Back works.
- [ ] Header title + subtitle match Screen 1/2.
- [ ] Fields: Student ID, Email, Password with leading icons; password obscure toggle.
- [ ] Password never sent in verification API / draft storage.
- [ ] Empty upload matches Screen 1 + `Upload Icon.png`.
- [ ] After pick: name, size, type icon, remove — matches Screen 2.
- [ ] CTA **Verify Student Status** submits via existing logic.
- [ ] Info card copy matches mock.
- [ ] Light/dark readable; no overflow with keyboard / long file names.
- [ ] Email still only requires non-empty until product adds format rules.

## Dependencies

- `update.md`
- `v0/01.verify_from.md`
- `v0/02.pending_verify.md`
- `v1/03.finance_home-v1.md`
- Auth / file service backend prompts

## Output

Deliver Student Verification form **v1 UI** matching Verification Screen 1 & 2 while preserving v0 security and submit routing.
