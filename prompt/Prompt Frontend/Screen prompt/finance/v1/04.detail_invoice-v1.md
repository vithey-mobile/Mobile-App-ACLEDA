# 04 - Finance Invoice Detail Prompt (v1)

Build the secure invoice / payment-detail surface opened from a Finance payment row.

> **Do not modify** `v0/04.detail_invoice.md`. This v1 file is the new UI direction.  
> **Visual source of truth:** `Prompt Frontend/screen image/finance/Transaction.png`  
> **Behavior baseline:** `v0/04.detail_invoice.md` (ownership, PDF rules, money types).  
> Pair with Finance Home v1: `v1/03.finance_home-v1.md`.

## Visual references

- Primary: `Prompt Frontend/screen image/finance/Transaction.png` (Paid + Pending states side by side)
- Legacy (v0): `preview invoice.png` — superseded for layout chrome

Treat **Transaction.png** as the source of truth for hierarchy, copy, and status coloring.

## Route / entry

Unchanged from v0:

- Large rounded modal bottom sheet (or dedicated detail route).
- Input: typed `PaymentInvoiceArgs(paymentId)`.
- Never authorize by list index, title, amount, or visible reference alone.
- Closing returns to the same Finance list position.

## Layout (v1 — match Transaction.png)

```text
┌────────────────────────────────────┐
│           Fee title (center)       │
│        Invoice reference (muted)   │
│  ┌──────────────────────────────┐  │
│  │ STATUS          DATE         │  │  tinted panel
│  │ [PAID pill]     Apr 04, 2026 │  │
│  └──────────────────────────────┘  │
│  BREAKDOWN                         │
│  Base Amount              $…       │
│  Processing Fee           $…       │
│  Late Charge              $…       │  accent color by state
│  ─────────────────────────────     │
│  Total Due                $…       │  accent color by state
│  [ Download PDF Invoice     ]      │
│  [ Report an Issue          ]      │
└────────────────────────────────────┘
```

### Header

- Centered **fee name** (bold, ~20–22).
- Centered **invoice / transaction reference** under title (muted, slightly tracked / uppercase-friendly).
- Optional drag handle; close control remains accessible (top corner or swipe dismiss).

### Status / date panel

- Full-width rounded panel with **state-tinted fill**:
  - **Paid** → soft teal / mint fill
  - **Pending** (unpaid) → soft peach / warm fill
  - **Overdue** → soft red / warm fill (same pattern as Pending, stronger emphasis)
- Left column:
  - Small muted **STATUS** label
  - Solid pill badge with **white** label text: `PAID` / `PENDING` / `OVERDUE`
    - Paid pill: brand teal
    - Pending pill: orange / warning
    - Overdue pill: error red
- Right column:
  - Small muted **DATE** label (not “Paid Date” / “Due Date” wording — mock uses **DATE**)
  - Formatted date in the **accent color** for that state (paidAt when Paid; dueAt when Pending/Overdue)

### Breakdown

- Section label **BREAKDOWN** (small, bold, muted, letter-spaced).
- Rows (label left, amount right):
  1. Base Amount
  2. Processing Fee
  3. Late Charge (singular copy per mock; map from `lateCharges`)
- Late Charge amount uses accent color (teal when Paid, red/orange when Pending/Overdue).
- Other row amounts use theme heading / body color.

### Total

- Thin divider.
- Left: **Total Due** (mock uses this label for both Paid and Pending — keep consistent unless product later prefers “Total Paid”).
- Right: large bold total in **accent color** (teal Paid, red Pending/Overdue).

### Actions

Two full-width primary (teal) buttons stacked:

1. **Download PDF Invoice** — download icon + label; loading / progress / error as v0.
2. **Report an Issue** — help / question icon + label; open support / report flow when available (placeholder snackbar acceptable until support route exists). Do not invent a backend.

## Color / theme rules

| State | Panel fill | Pill | Date + Late Charge + Total |
| --- | --- | --- | --- |
| Paid | Soft teal | Solid teal, white text | Teal / primary |
| Pending | Soft peach | Solid orange, white text | Orange date; red total & late |
| Overdue | Soft red tint | Solid red, white text | Red accents |

Theme-aware surfaces for the sheet itself (light/dark). Accent colors stay brand-readable on both modes.

## Data / PDF / security / a11y

Same as v0:

- Load owned detail via `GET /api/v1/payments/{paymentId}`.
- Authoritative breakdown; minor-unit money; verify `base + processing + late ≈ total`.
- **Finance Home list amount must equal this total** (not base alone).
- Authenticated PDF download; no public permanent URLs; no logging of PII/amounts/refs.
- Focus trap, 44×44 targets, status not by color alone (pill text + labels).

## Architecture

```text
lib/modules/finance/widgets/
  invoice_preview_sheet.dart   # v1 UI
lib/data/models/
  payment_invoice_model.dart
```

## Testing and acceptance (v1)

- [ ] Matches `Transaction.png` for Paid and Pending: centered title/ref, STATUS/DATE panel, BREAKDOWN, Total Due accent, two teal CTAs.
- [ ] Overdue uses the same structure with error accents.
- [ ] Download + Report actions present; download still authenticated.
- [ ] Closing preserves Finance Home scroll/expand state.
- [ ] Light/dark readable; no overflow with long fee names / references.

## Dependencies

- `update.md` (Finance Home UI context)
- `v0/04.detail_invoice.md`
- `v1/03.finance_home-v1.md`
- Finance / file service backend prompts

## Output

Deliver invoice detail v1 UI matching Transaction.png while preserving v0 ownership, money accuracy, and PDF security rules.
