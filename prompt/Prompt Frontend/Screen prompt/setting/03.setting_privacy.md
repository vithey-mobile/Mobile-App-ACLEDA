# 03 - Settings Privacy Prompt

Build the **Privacy** settings screen for Vithey App in Flutter, matching:

- `Prompt Frontend/screen image/setting/setting_privacy.png`

## Quick info

| Field | Value |
|---|---|
| Route | `Routes.SETTING_PRIVACY` |
| Flutter module | `lib/modules/settings/privacy/` or `lib/modules/settings/` |
| Backend service | `user-profile-service` |
| Auth required | Yes |

## Goal

Let users control basic privacy preferences and understand data protection.

## Visual requirements

Match `setting_privacy.png`, aligned with Settings Home styling (`01.setting_home.md`):

- White/light background.
- App bar:
  - Back arrow on left.
  - Bold title: **Privacy**.
  - Thin divider below.
- Header section:
  - Title: **Privacy Settings**
  - Subtitle: **Control how your data is used and shared**
- Rounded grouped card with soft shadow and three switch rows:
  - Profile Visibility
  - Data Sharing
  - Activity Tracking
- Each switch row:
  - Left outline icon with **primary color** (`Theme.of(context).colorScheme.primary`), size ~22.
  - Bold title.
  - Grey/muted subtitle below title.
  - Right **compact switch** — same size and component as the **Dark Mode** toggle on Settings Home (`SettingsSwitchTile` / `shad.Switch`). Do **not** use the larger Material `Switch.adaptive`.
  - Row vertical padding should feel the same height as other settings rows (~14px vertical padding).
  - Inset divider between rows (not full-bleed edge-to-edge).
- Data Protection information card:
  - Primary-colored shield icon.
  - Title: **Data Protection**
  - Body copy exactly: `Your data is encrypted and securely stored. We never share your personal information with third parties without your consent.`
  - Primary-colored link: **Learn more about our privacy practices →**
- Bottom navigation remains visible with **Profile** selected, matching the app shell.

## Preferences model

```dart
class PrivacySettings {
  final bool profileVisible;
  final bool dataSharing;
  final bool activityTracking;
}
```

If backend settings are nested, store under:

```json
{
  "privacy": {
    "profile_visible": true,
    "data_sharing": false,
    "activity_tracking": false
  }
}
```

## Architecture

```text
lib/modules/settings/privacy/
  privacy_settings_screen.dart
  privacy_settings_controller.dart
  privacy_settings_binding.dart
  widgets/
    privacy_switch_card.dart
    privacy_switch_tile.dart
    data_protection_card.dart
```

Reuse shared settings styling where possible:

- Switch control: same compact `shad.Switch` pattern as `lib/modules/settings/widgets/settings_switch_tile.dart` (Dark Mode row).
- Icons: primary color, consistent with `settings_menu_tile.dart` and Account settings screens.
- Scaffold: reuse `settings_scaffold.dart` for app bar + divider.

## Controller behavior

- `loadPrivacySettings()`:
  - Load local cache first.
  - Fetch `GET /users/me/settings`.
- `toggleProfileVisibility(bool value)`:
  - Optimistically update UI.
  - Persist to `PATCH /users/me/settings`.
  - Roll back on failure.
- `toggleDataSharing(bool value)` and `toggleActivityTracking(bool value)` same behavior.
- `openPrivacyPractices()`:
  - Open in-app article route if available.
  - Otherwise use `url_launcher` to open a configured privacy URL.

## API

| Method | Path | Notes |
|---|---|---|
| `GET` | `/api/v1/users/me/settings` | Load privacy preferences |
| `PATCH` | `/api/v1/users/me/settings` | Save changed preference |

Use `Prompt Frontend/api-intergration/integration-contract.md`.

## UX rules

- Switch changes are optimistic but must roll back on API failure.
- Privacy toggles must use the **same compact switch size** as Dark Mode on Settings Home — not oversized platform switches.
- Row icons use **primary color**, not grey/muted.
- Do not claim data is shared when toggles are off.
- Keep explanatory text short and readable.
- If a privacy preference is unsupported by backend, hide it behind local-only flag or disable with clear copy.

## Testing

- Screen matches reference grouping and copy.
- Switch rows use compact toggles matching Settings Home Dark Mode.
- All row icons and Data Protection shield/link use primary color.
- Each switch updates controller state.
- Failed API update rolls back only the affected switch.
- Data Protection link has a real action or is hidden.

## Output

Complete Privacy settings screen matching `setting_privacy.png`, with persistent settings through `user-profile-service`.

