# Settings Security Prompt

> Legacy filename kept for compatibility. Preferred route file order: see `README.md`.

Build the **Security** settings screen for Vithey App in Flutter, matching:

- `Prompt Frontend/screen image/setting/setting_security.png`

## Quick info

| Field | Value |
|---|---|
| Route | `Routes.SETTING_SECURITY` |
| Flutter module | `lib/modules/settings/security/` or `lib/modules/settings/` |
| Backend services | `auth-service`, `user-profile-service` |
| Auth required | Yes |

## Goal

Let users manage account security: change password, two-factor authentication, biometric login, active sessions, and sign out all devices.

## Visual requirements

Match `setting_security.png`:

- White background.
- App bar:
  - Back arrow.
  - Bold title: **Security**.
  - Divider below.
- Section heading:
  - **Security Settings**
  - Subtitle: **Manage your account security and authentication**
- Rounded security card:
  - Change Password row with lock icon and chevron.
  - Two-Factor Authentication row with key icon and switch.
  - Biometric Login row with phone icon and switch.
- Active Sessions card:
  - Teal shield icon.
  - Title: **Active Sessions**
  - Current Device row, subtitle **Last active: Just now**
  - Right status **Active** in teal.
- Danger card:
  - Pale red header.
  - Red info icon.
  - Title: **Sign Out All Devices**
  - Subtitle: **Manage your active sessions**
  - Body copy:
    - This will sign you out from all devices except this one.
    - You'll need to sign in again on other devices.
  - Full-width red outline/pale button: **Sign Out Everywhere**
- Bottom navigation remains visible with Profile selected.

## Architecture

```text
lib/modules/settings/security/
  security_settings_screen.dart
  security_settings_controller.dart
  security_settings_binding.dart
  widgets/
    security_option_card.dart
    active_sessions_card.dart
    sign_out_all_devices_card.dart
```

## Controller behavior

- `openChangePassword()` → `Get.toNamed(Routes.SETTING_CHANGE_PASSWORD)`.
- `toggleTwoFactor(bool value)`:
  - If backend supports 2FA, open enrollment/disable flow.
  - If unsupported, show “Two-factor authentication is coming soon” and keep switch off.
- `toggleBiometric(bool value)`:
  - Use local biometric availability check.
  - Store local preference only after successful local auth.
- `loadActiveSessions()`:
  - Use current device local info until backend exposes sessions endpoint.
- `signOutAllDevices()`:
  - Confirm with `ConfirmDialog`.
  - Call backend endpoint if available.
  - Otherwise show unavailable message and do not fake success.

## API

Current documented API:

| Method | Path | Notes |
|---|---|---|
| `POST` | `/api/v1/auth/logout` | Current-device logout only |

Recommended backend additions for full security features:

| Method | Path | Notes |
|---|---|---|
| `PATCH` | `/api/v1/auth/me/password` | Change current password |
| `GET` | `/api/v1/auth/sessions` | List active sessions |
| `DELETE` | `/api/v1/auth/sessions` | Sign out all other devices |
| `PATCH` | `/api/v1/auth/me/2fa` | Enable/disable 2FA |

Do not implement fake remote session behavior if backend endpoints do not exist.

## Security rules

- Never log passwords, tokens, or session IDs.
- Biometric toggle is local app unlock only unless backend supports device binding.
- Sign Out Everywhere must not sign out the current route until backend confirms or clearly reports unsupported.

## Testing

- Change Password row navigates correctly.
- Toggles do not lie about unsupported backend features.
- Sign Out Everywhere confirmation works.
- Active Sessions card renders current device.

## Output

Complete Security screen matching `setting_security.png`, with safe behavior for implemented and not-yet-supported security APIs.
