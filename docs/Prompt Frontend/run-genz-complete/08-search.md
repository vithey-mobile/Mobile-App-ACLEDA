# GLM 5.3 Flash — Prompt 08 — Search (GenZ)

Copy everything below `---` into a **new** chat. Run after Prompt 00. Parallel OK with other modules.

---

You are a Flutter UI agent. Restyle **search module only** to GenZ smart screens.

## Own only

```text
vithey_app/lib/modules/search/**
```

## Requirements

1. App bar search → `VitheySearchPill`.
2. Section chips / filters → `VitheyFilterChips`.
3. Result tiles (people / posts / jobs / videos): radius 16, rounder trailing icons.
4. See-all screens match.
5. Empty / error / loading kit.
6. Action sheets for results → `showVitheyActionSheet`.
7. Do not break search repository / mock fixtures.

## Stop when

Search GenZ-consistent; analyze clean.
