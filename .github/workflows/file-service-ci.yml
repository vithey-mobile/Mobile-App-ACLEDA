name: file-service CI

on:
  push:
    branches: [main, Prompt, dev, kimheang]
    paths:
      - 'backend/pom.xml'
      - 'backend/infrastructure/config-repo/file-service.yml'
      - 'backend/infrastructure/config-server/**'
      - 'backend/infrastructure/eureka-server/**'
      - 'backend/services/file-service/**'
      - '.github/workflows/file-service-ci.yml'
  pull_request:
    branches: [main, Prompt, dev, kimheang]
    paths:
      - 'backend/pom.xml'
      - 'backend/infrastructure/config-repo/file-service.yml'
      - 'backend/infrastructure/config-server/**'
      - 'backend/infrastructure/eureka-server/**'
      - 'backend/services/file-service/**'
      - '.github/workflows/file-service-ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - name: Run file-service tests
        run: mvn -B -f backend/pom.xml -pl services/file-service -am test
      - name: Upload test reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: file-service-test-reports
          path: backend/services/file-service/target/surefire-reports/

  docker-build:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - name: Build file-service image
        run: docker build -f backend/services/file-service/Dockerfile --build-arg SERVICE_PORT=8083 -t vithey-file-service:ci backend

  compose-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate file-service compose
        run: docker compose -f backend/services/file-service/docker-compose.yml config
