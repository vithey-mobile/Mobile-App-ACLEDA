name: content-service CI

on:
  push:
    branches: [main, Prompt, develop]
    paths:
      - 'backend/pom.xml'
      - 'backend/infrastructure/config-repo/content-service.yml'
      - 'backend/infrastructure/config-server/**'
      - 'backend/infrastructure/eureka-server/**'
      - 'backend/services/content-service/**'
      - '.github/workflows/content-service-ci.yml'
  pull_request:
    branches: [main, Prompt, develop]
    paths:
      - 'backend/pom.xml'
      - 'backend/infrastructure/config-repo/content-service.yml'
      - 'backend/infrastructure/config-server/**'
      - 'backend/infrastructure/eureka-server/**'
      - 'backend/services/content-service/**'
      - '.github/workflows/content-service-ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - name: Run content-service tests
        run: mvn -B -f backend/pom.xml -pl services/content-service -am test
      - name: Upload test reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: content-service-test-reports
          path: backend/services/content-service/target/surefire-reports/

  docker-build:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - name: Build content-service image
        run: docker build -f backend/services/content-service/Dockerfile --build-arg SERVICE_PORT=8084 -t vithey-content-service:ci backend

  compose-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate content-service compose
        run: docker compose -f backend/services/content-service/docker-compose.yml config
