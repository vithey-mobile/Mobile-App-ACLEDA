name: finance-service CI

on:
  push:
    branches: [main, Prompt, dev, kimheang]
    paths:
      - 'backend/pom.xml'
      - 'backend/infrastructure/config-repo/finance-service.yml'
      - 'backend/infrastructure/config-server/**'
      - 'backend/infrastructure/eureka-server/**'
      - 'backend/services/finance-service/**'
      - '.github/workflows/finance-service-ci.yml'
  pull_request:
    branches: [main, Prompt, dev, kimheang]
    paths:
      - 'backend/pom.xml'
      - 'backend/infrastructure/config-repo/finance-service.yml'
      - 'backend/infrastructure/config-server/**'
      - 'backend/infrastructure/eureka-server/**'
      - 'backend/services/finance-service/**'
      - '.github/workflows/finance-service-ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - name: Run finance-service tests
        run: mvn -B -f backend/pom.xml -pl services/finance-service -am test
      - name: Upload test reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: finance-service-test-reports
          path: backend/services/finance-service/target/surefire-reports/

  docker-build:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - name: Build finance-service image
        run: docker build -f backend/services/finance-service/Dockerfile --build-arg SERVICE_PORT=8086 -t vithey-finance-service:ci backend

  compose-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate finance-service compose
        run: docker compose -f backend/services/finance-service/docker-compose.yml config
