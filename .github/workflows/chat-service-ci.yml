name: chat-service CI

on:
  push:
    branches: [main, Prompt, dev, kimheang]
    paths:
      - 'backend/pom.xml'
      - 'backend/infrastructure/config-repo/chat-service.yml'
      - 'backend/infrastructure/config-server/**'
      - 'backend/infrastructure/eureka-server/**'
      - 'backend/services/chat-service/**'
      - 'backend/services/api-gateway/**'
      - '.github/workflows/chat-service-ci.yml'
  pull_request:
    branches: [main, Prompt, dev, kimheang]
    paths:
      - 'backend/pom.xml'
      - 'backend/infrastructure/config-repo/chat-service.yml'
      - 'backend/infrastructure/config-server/**'
      - 'backend/infrastructure/eureka-server/**'
      - 'backend/services/chat-service/**'
      - 'backend/services/api-gateway/**'
      - '.github/workflows/chat-service-ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - name: Run chat-service tests
        run: mvn -B -f backend/pom.xml -pl services/chat-service -am test
      - name: Upload test reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: chat-service-test-reports
          path: backend/services/chat-service/target/surefire-reports/

  docker-build:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - name: Build chat-service image
        run: docker build -f backend/services/chat-service/Dockerfile --build-arg SERVICE_PORT=8087 -t vithey-chat-service:ci backend

  compose-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate chat-service compose
        run: docker compose -f backend/services/chat-service/docker-compose.yml config
