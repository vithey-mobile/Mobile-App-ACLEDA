name: auth-service CI

on:
  push:
    branches: [main, Prompt, develop]
    paths:
      - 'backend/pom.xml'
      - 'backend/infrastructure/config-repo/auth-service.yml'
      - 'backend/infrastructure/config-server/**'
      - 'backend/infrastructure/eureka-server/**'
      - 'backend/services/auth-service/**'
      - '.github/workflows/auth-service-ci.yml'
  pull_request:
    branches: [main, Prompt, develop]
    paths:
      - 'backend/pom.xml'
      - 'backend/infrastructure/config-repo/auth-service.yml'
      - 'backend/infrastructure/config-server/**'
      - 'backend/infrastructure/eureka-server/**'
      - 'backend/services/auth-service/**'
      - '.github/workflows/auth-service-ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - name: Run auth-service tests
        run: mvn -B -f backend/pom.xml -pl services/auth-service -am test
      - name: Upload test reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: auth-service-test-reports
          path: backend/services/auth-service/target/surefire-reports/

  docker-build:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - name: Build auth-service image
        run: docker build -f backend/services/auth-service/Dockerfile --build-arg SERVICE_PORT=8081 -t vithey-auth-service:ci backend

  compose-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate auth-service compose
        run: docker compose -f backend/services/auth-service/docker-compose.yml config
