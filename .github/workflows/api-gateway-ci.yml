name: api-gateway CI

on:
  push:
    branches: [main, Prompt, develop]
    paths:
      - 'backend/pom.xml'
      - 'backend/infrastructure/config-repo/api-gateway.yml'
      - 'backend/infrastructure/config-server/**'
      - 'backend/infrastructure/eureka-server/**'
      - 'backend/services/api-gateway/**'
      - '.github/workflows/api-gateway-ci.yml'
  pull_request:
    branches: [main, Prompt, develop]
    paths:
      - 'backend/pom.xml'
      - 'backend/infrastructure/config-repo/api-gateway.yml'
      - 'backend/infrastructure/config-server/**'
      - 'backend/infrastructure/eureka-server/**'
      - 'backend/services/api-gateway/**'
      - '.github/workflows/api-gateway-ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - name: Run api-gateway tests
        run: mvn -B -f backend/pom.xml -pl services/api-gateway -am test
      - name: Upload test reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: api-gateway-test-reports
          path: backend/services/api-gateway/target/surefire-reports/

  docker-build:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - name: Build api-gateway image
        run: docker build -f backend/services/api-gateway/Dockerfile --build-arg SERVICE_PORT=8080 -t vithey-api-gateway:ci backend

  compose-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate api-gateway compose
        run: docker compose -f backend/services/api-gateway/docker-compose.yml config
