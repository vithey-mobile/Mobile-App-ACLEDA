name: user-profile-service CI

on:
  push:
    branches: [main, Prompt, develop]
    paths:
      - 'backend/pom.xml'
      - 'backend/infrastructure/config-repo/user-profile-service.yml'
      - 'backend/infrastructure/config-server/**'
      - 'backend/infrastructure/eureka-server/**'
      - 'backend/services/user-profile-service/**'
      - '.github/workflows/user-profile-service-ci.yml'
  pull_request:
    branches: [main, Prompt, develop]
    paths:
      - 'backend/pom.xml'
      - 'backend/infrastructure/config-repo/user-profile-service.yml'
      - 'backend/infrastructure/config-server/**'
      - 'backend/infrastructure/eureka-server/**'
      - 'backend/services/user-profile-service/**'
      - '.github/workflows/user-profile-service-ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - name: Run user-profile-service tests
        run: mvn -B -f backend/pom.xml -pl services/user-profile-service -am test
      - name: Upload test reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: user-profile-service-test-reports
          path: backend/services/user-profile-service/target/surefire-reports/

  docker-build:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - name: Build user-profile-service image
        run: docker build -f backend/services/user-profile-service/Dockerfile --build-arg SERVICE_PORT=8082 -t vithey-user-profile-service:ci backend

  compose-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate user-profile-service compose
        run: docker compose -f backend/services/user-profile-service/docker-compose.yml config
