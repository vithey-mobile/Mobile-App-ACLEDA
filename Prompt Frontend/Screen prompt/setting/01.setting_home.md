# 01 - Settings Home Prompt

Build the **Settings Home** screen for Vithey App in Flutter, matching:

- `Prompt Frontend/screen image/setting/setting_home.png`

## Quick info

| Field | Value |
|---|---|
| Route | `Routes.SETTINGS` |
| Flutter module | `lib/modules/settings/` |
| Backend services | `user-profile-service`, `auth-service` |
| Auth required | Yes |
| Entry point | Profile tab/settings gear |

## Goal

Show a grouped settings menu where the user can open Account, Privacy, Language, Notifications, Security, Help Center, About, toggle Dark Mode, and log out.

## Visual requirements

Match `setting_home.png`:

- White/light background.
- Safe-area top with app bar:
  - Back arrow on left.
  - Bold title: **Settings**.
  - Thin divider below.
- Section labels:
  - **Preferences**
  - **System**
  - **Support**
- Rows are simple, full-width, white cards/tiles with:
  - Left outline icon.
  - Label.
  - Optional subtitle for Language (`English (US)`).
  - Right chevron for navigable rows.
- Dark Mode row uses a switch, not a chevron.
- Logout is a full-width pale-red rounded button with red icon/text.
- Bottom navigation remains visible with **Profile** selected, matching the app shell.

## Menu structure

| Section | Row | Action |
|---|---|---|
| Preferences | Account | `Routes.SETTING_ACCOUNT` |
| Preferences | Privacy | `Routes.SETTING_PRIVACY` |
| Preferences | Language / English (US) | Open language picker bottom sheet |
| System | Notifications | `Routes.NOTIFICATION` or notification preferences when implemented |
| System | Security | `Routes.SETTING_SECURITY` |
| System | Dark Mode | Toggle local theme immediately |
| Support | Help Center | `Routes.SETTING_HELP_CENTER` |
| Support | About | `Routes.SETTING_ABOUT` |
| — | Logout | Confirm → auth logout → Auth |

## Architecture

```text
lib/modules/settings/
  settings_home_screen.dart
  settings_controller.dart
  settings_binding.dart
  widgets/
    settings_section_label.dart
    settings_menu_tile.dart
    settings_switch_tile.dart
    settings_logout_button.dart
```

## Controller behavior

- `loadSettings()` reads local theme/language first, then optionally `GET /users/me/settings`.
- `toggleDarkMode(bool value)` updates `Get.changeThemeMode`, persists locally, and syncs with `PATCH /users/me/settings`.
- `changeLanguage()` opens a bottom sheet with `English (US)` and `Khmer`.
- `logout()`:
  1. Show `ConfirmDialog`.
  2. Call `AuthRepository.logout()` best effort.
  3. Clear secure/local session data.
  4. `Get.offAllNamed(Routes.AUTH)`.

## API

| Method | Path | Notes |
|---|---|---|
| `GET` | `/api/v1/users/me/settings` | Load remote preferences |
| `PATCH` | `/api/v1/users/me/settings` | Save theme/language/notifications |
| `POST` | `/api/v1/auth/logout` | Revoke refresh token |

Use `Prompt Frontend/api-intergration/integration-contract.md`.

## Testing

- Settings rows render in the same sections as the reference.
- Dark mode switch persists and updates app theme.
- Logout confirms and clears session.
- All navigable rows call the correct route.

## Output

Complete Settings Home screen matching `setting_home.png` with working navigation, dark mode, language persistence, and logout.
